/** @format */

const divide = function (a, b) {
  return a / b;
};

console.log(divide(2000, 100));

const square = (number) => {
  return number * number;
};

const add = (a, b) => {
  return a + b;
};
